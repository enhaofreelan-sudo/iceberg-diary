# 冰山日记 - 项目开发规划与文件系统分类

本项目基于萨提亚冰山理论，前后端分离（通过单体静态挂载实现部署）。以下为当前最新项目文件的层级与职能分类矩阵，同时作为基础架构指南以备未来持续迭代参考。

## 一、 当前文件模块分类架构

### 1. 核心前端页面门户 (`public/` 目录)
构成系统的直接交互端点，采用轻量原生 HTML/JS/CSS 单文件架构。
- **`index.html`**：业务核心主应用页面。面向学生与家长的“冰山日记”动态交互填写端，集成深度解析、Canvas长图生成与本地持久化。
- **`login.html`**：项目集中登录及鉴权入口。融合双模式自适应家长注册流程（入校验证及脱机填表）。
- **`teacher.html`**：教师端独立管理总台。用于日记多维透视聚合、家长用户管理干预及紧急密码重置控制权限。

### 2. 前端静态资源与网络规范 (`public/` 目录)
提升界面观感与外部联通标准。
- **`cover.png` / `cover.webp`**：系统主观封面与分享导图，双格式无缝适配兼容现代浏览器加速呈现。
- **`favicon.png`**：浏览器地址及标签专属品牌图标。
- **`robots.txt`**：全站 SEO 收录爬取管控规范基础文件。

### 3. 项目与配置约束基石 (项目根目录)
- **`README.md`**：系统官方项目白皮书，涵盖技术栈体系、部署指令及历史变更迭代全纪录。
- **`.gitignore`**：代码同步过滤网，阻断 `data/` 、`config.py` 及 Python 缓存等敏感资产上云外流。

### 4. 后端接口服务枢纽 (`server/` 目录)
驱动业务生命线逻辑，接管并流转用户凭证和数据资产。
- **`server/app.py`**：FastAPI 后端心脏引擎。挂载静态前台并辐射全部 RESTful API、承接飞书表单远程推流作业流。
- **`server/auth.py`**：鉴权加密保险库。执行 JWT 生命周期签署颁发和 bcrypt 用户口令脱敏摘要验证。

### 5. 数据库链接与演进工具链 (`server/` 目录)
- **`server/database.py`**：本地 SQLite 持久化接入基建，封装原生调用为安全 Context 链路管理器。
- **`server/init_db.py`**：初始基座搭建。首次启动创建所有核心物理表空间并烧录创始超级管理员账号体系。
- **`server/migrate.py`**：自动架构迁移履带机。确保升级新版结构时底层旧数据完整映射至新结构中安全软着陆。
- **`server/add_reg_fields.py`**：特定业务扩容热插拔脚本，为业务延展补充新用户源字段通道。
- **`server/clean_empty.py`**：数据清洗拦截组件。自动筛除系统运行或旧版本迁移遗留的结构型残次死数据空记录。

### 6. 服务环境与凭证载体 (`server/` 目录)
- **`server/config.py`** (隐形保护)：系统深层配置文件，收拢飞书多维凭据与本地进程端口参数。
- **`server/config.example.py`**：配置骨架克隆体模板，为新环境搭建提供字段蓝本示范。
- **`server/requirements.txt`**：Python 部署依赖声明清单，限定环境重构所必要的引擎插件与版本线。

---

## 二、 现阶系统部署与阶段规划说明

* **数据平滑**：由原始飞书纯 Webhook 提交架构，成功演进并平滑迁移至本地 SQLite 后端固化存储引擎。
* **用户隔离**：家长层级与管理员(老师)层级数据边界确立，并由 JWT 建立强校验屏障闭环。
* **后期规划展望**：
  1. 定期备份机制脚本构建，防止本地单点数据库物理受损。
  2. Canvas 模块前端性能压缩进一步优化，加速低端手机上生成。
  3. API 日志分析监控集成，跟踪家长访问高峰流向。
